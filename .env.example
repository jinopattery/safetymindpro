# Application Settings
APP_NAME=SafetyMindPro
APP_VERSION=1.0.0
ENVIRONMENT=development

# Server Configuration
HOST=0.0.0.0
PORT=8000
RELOAD=true

# Database
DATABASE_URL=sqlite+aiosqlite:///./safetymindpro.db

# Security
SECRET_KEY=your-secret-key-here-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000

# Email Verification
# Set to false to skip email verification entirely (e.g. internal tools).
# When true and no SMTP is configured, the verification link is shown
# directly on-screen after signup – no mail server required.
EMAIL_VERIFICATION_REQUIRED=true
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
APP_BASE_URL=http://localhost:3000

# Domain Isolation
# Comma-separated list of domain names to expose in this deployment.
# When empty (default), all registered domains are available.
# Examples:
#   ENABLED_DOMAINS=automotive          (automotive-only build)
#   ENABLED_DOMAINS=finance,financial   (finance/trading build)
#   ENABLED_DOMAINS=                    (all domains – default)
ENABLED_DOMAINS=

# Frontend Domain Isolation (React build-time variable – set in frontend/.env)
# Same semantics as ENABLED_DOMAINS above but applied client-side.
# REACT_APP_ENABLED_DOMAINS=automotive

# GDPR / Privacy
PRIVACY_POLICY_VERSION=1.0